% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_occurrence_data.R
\name{get_occurrence_data}
\alias{get_occurrence_data}
\title{Get Occurrence Data}
\usage{
get_occurrence_data(
  data = NULL,
  source_type = c("local_csv", "data_frame", "gbif"),
  gbif_zip_url = NULL,
  download_dir = tempdir(),
  sep = ","
)
}
\arguments{
\item{data}{Input data. Can be:
\itemize{
\item File path to a local CSV file (when source_type = "local_csv").
\item A data frame already loaded in R (when source_type = "data_frame").
\item NULL for GBIF download (when source_type = "gbif").
}}

\item{source_type}{Source type of the data. One of:
\itemize{
\item "local_csv": Load data from a CSV file.
\item "data_frame": Use an existing data frame in R.
\item "gbif": Download and process data from a GBIF ZIP URL.
}}

\item{gbif_zip_url}{URL of the GBIF ZIP file (required when source_type = "gbif").}

\item{download_dir}{Directory to save the downloaded GBIF data (default: temporary directory).}

\item{sep}{Separator for reading CSV files (default: ",").}
}
\value{
A processed data frame in long or wide format. Returns:
\itemize{
\item "long format" if sp_name and either pa or abundance columns are detected.
\item "wide format" if species columns (sp_*) are present.
}
}
\description{
This function imports and processes biodiversity occurrence data from various sources.
It supports local CSV files, data frames already in R, or GBIF downloads (in ZIP format).
}
\examples{
# Load data from a local CSV file:
# local_data <- get_occurrence_data(data = "path/to/local.csv", source_type = "local_csv")

# Use an existing data frame:
# df_data <- get_occurrence_data(data = local_data, source_type = "data_frame")

# Download GBIF data:
# gbif_data <- get_occurrence_data(source_type = "gbif",
#                                  gbif_zip_url = "https://api.gbif.org/v1/occurrence/download/request/0038969-240906103802322.zip",
#                                  download_dir = "path/to/download/dir")
}
